# 项目概述

**react-spreadsheet** 是一个面向 **React** 的表格（电子表格）组件库：在网页里提供类似 Excel 的网格编辑体验，强调**简单接入**、**可定制**，并在常见场景下保持较好性能。

本仓库在此基础上扩展了开发与演示能力（例如通过 Storybook 进行组件开发与预览），并可选配合后端服务实现透视表等需要服务端计算的能力。

如需安装、快速开始与完整说明，请参阅仓库根目录的 `readme.md`。
